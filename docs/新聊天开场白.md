我们继续推进 `bn_research_core`，但当前阶段的主线已经从“继续无限细挖 Snapback/SAB 桶”切换到“让 SABC 毕业 + 开启新策略 Top1 Hunter”。

以下 3 份文本内容仍然构成当前对话唯一基线，请严格遵守：

1. `PROJECT_BASELINE.md`
2. `STANDARD_PATCH_FRAMEWORK.md`
3. `CURRENT_STATE.md`

优先级固定为：

1. `PROJECT_BASELINE.md`
2. `STANDARD_PATCH_FRAMEWORK.md`
3. `CURRENT_STATE.md`

请先完全按这三份内容协作，不要跳回旧主线，不要重复已完成 patch，不要把已经收口的 SAB 深挖重新拉回第一优先级。

# 项目基线（宪法）
（`PROJECT_BASELINE.md`）

## 0. 总则
0.1 本基线适用于本项目内全部策略、全部 `sim / live / audit / patch / review / 对话协作`。  
0.2 若某策略需要例外，必须显式写入该策略文档；**未写明即禁止例外**。  
0.3 凡本基线未显式允许的行为，一律视为禁止。  

## 1. 公共语义

### 1.1 K线语义
1.1.1 全部 K 线统一分为：  
- `CB = Current Bar = 当前 bar = 编号 0`  
- `HBs = History Bars = 全部已收盘 bars = 编号 1..N`  

1.1.2 `CB` 永远只有唯一一根，OHLCV 会变化。  
1.1.3 `HBs` 全部已收盘，OHLCV 已固化不变。  
1.1.4 所有策略都必须在 **CB 时刻** 观察 **HBs**。  

### 1.2 时间字段语义
1.2.1 `signal_time` 只能表示信号生产时间。  
1.2.2 `signal_time` 必须严格等于 `CB` 的时间。  
1.2.3 `entry_time` 只能表示进场时间。  
1.2.4 `entry_time` 必须严格等于 `signal_time`。  
1.2.5 对本项目全部策略，`signal_time = entry_time = CB`。  

### 1.3 策略内部时间锚点
1.3.1 某些策略可能定义自己的内部结构时间锚点，例如 `A / B / C / S`。  
1.3.2 内部结构时间锚点（例如 `A / B / C / S`）必须全部归属于 HBs，即全部属于过去时。  
1.3.3 这类内部时间锚点**不是**项目公共语义，禁止默认推广到全部策略。  
1.3.4 若某策略存在内部时间锚点，必须在该策略文档中显式定义其名称、含义、对应 bar 和对应字段。  
1.3.5 未在策略文档中显式定义的内部时间锚点，不得假定存在。  

### 1.4 价格语义
1.4.1 价格统一区分为：`contract` 与 `idx`。  
1.4.2 凡涉及价格字段，必须明确属于哪一类。  
1.4.3 禁止混用或偷换 `contract` 与 `idx`。  

### 1.5 配置语义
1.5.1 所有策略配置统一采用“配置段（section）”概念。  
1.5.2 当前公共配置段为：  
- `strategy_name`  
- `runtime`  
- `universe`  
- `structure`  
- `exit_policy`  
- `risk_controls`  

1.5.3 新增配置段时，必须显式说明职责；禁止隐式扩展旧段语义。  

### 1.6 sim / live 关系
1.6.1 `sim` 是策略语义基线。  
1.6.2 `live` 必须向 `sim` 对齐。  
1.6.3 若发现 `sim` 语义错误，必须先修 `sim`，再重跑验证，再要求 `live` 对齐。  
1.6.4 禁止用 `live` 现实限制掩盖 `sim` 语义错误。  

### 1.7 方向语义
1.7.1 本项目当前及未来统一只允许 LONG，不允许 SHORT。  
1.7.2 本项目不定义 SHORT 语义，不实现 SHORT 代码路径，不接受以 SHORT 为目标的 patch、review、audit、sim、live 设计。  
1.7.3 凡涉及方向的分析、对话、方案、代码实现、审计与交付，只讨论 LONG。  
1.7.4 未显式声明为 LONG-only 的字段、分支、行为，若引入 SHORT 语义，一律视为违规。  
1.7.5 禁止以“多空对称”“通用化”“未来扩展”为理由引入 SHORT 相关语义、字段、分支或实现。  

### 1.8 策略账本语义
1.8.1 策略账本分为：  
- 当前账本  
- 历史账本  

1.8.2 当前账本定义为：  
- 仓位  
- 当前委托  
  - 基础单  
  - 条件委托  

1.8.3 历史账本定义为：  
- 历史委托  
- 历史成交  
- 资金流水  
- 仓位历史记录  

1.8.4 策略账本同时区分为：  
- 本地账本  
- BN账本  

1.8.5 本地当前账本可记为 `LCL = Local Current Ledger`。  
1.8.6 BN当前账本可记为 `BCL = Binance Current Ledger`。  

1.8.7 当前账本一致性必须区分两个方向：  
- `BN → Local`：BN当前账本有，而本地当前账本没有  
- `Local → BN`：本地当前账本有，而BN当前账本没有  

1.8.8 凡涉及“账本对齐”“账本一致性”“orphan”“reconcile”的分析、设计、审计与实现，若未显式说明范围，默认只讨论**当前账本**，不得偷换为历史账本。  

1.8.9 凡涉及当前账本对象，必须明确对象层级，不得用含混表述混淆：  
- 仓位  
- 当前委托  
  - 基础单  
  - 条件委托  

1.8.10 `audit_consumer_orphan_exchange_activity(...)` 的职责，定义为：  
以 `BCL` 为基线，审计是否存在“`BCL` 中已有、而 `LCL` 中未记录”的 orphan 场景；该职责只覆盖 `BN → Local` 缺失方向，不等于完整账本对齐。  

1.8.11 完整的当前账本对齐，目标定义为：  
以 `BPOS + BWO` 为当前账本基线，使 `LPOS + LWO` 对齐基线。  
其中：  
- `BPOS = Binance Positions`  
- `BWO = Binance Working Orders`  
- `LPOS = Local Positions`  
- `LWO = Local Working Orders`  

## 2. 行动纪律（铁律）

### 2.1 事实优先
2.1.1 代码、日志、落盘文件、运行结果才是事实。  
2.1.2 一切分析、结论、方案必须基于事实。  
2.1.3 禁止臆测。  
2.1.4 缺少事实时，必须先补齐事实，再下结论。  

### 2.2 fail-fast
2.2.1 必须坚持 `fail_fast`。  
2.2.2 禁止带病运行。  
2.2.3 配置缺失、字段缺失、语义冲突、数据异常时，必须中断并报错。  

### 2.3 禁止兜底 / 禁止硬编码 / 禁止伪兼容
2.3.1 禁止默认值兜底掩盖错误。  
2.3.2 配置必须显式提供；缺失就是错误。  
2.3.3 禁止业务语义硬编码。  
2.3.4 临时硬编码调试完成后必须删除。  
2.3.5 禁止向下兼容破坏当前业务语义。  
2.3.6 严禁兼容旧字段、旧字段残留、多套语义并存。  
2.3.7 一条语义只能对应一套字段、一套实现。  

### 2.4 patch 协作
2.4.1 正式 patch 的进入条件、分类约束、交付顺序、最小修改原则，统一严格服从 `STANDARD_PATCH_FRAMEWORK.md`。  

### 2.5 表达纪律
2.5.1 提问和回答必须精准、简练。  
2.5.2 不遗漏关键线索。  
2.5.3 不发散、不啰嗦。  
2.5.4 先统一语义，再审代码。  

# 标准化 Patch 协作框架
（`STANDARD_PATCH_FRAMEWORK.md`）

## 0. 文档定位
本文件用于规定 **Patch 协作的唯一标准流程**。  
它不替代 `PROJECT_BASELINE.md`。  
若发生冲突，优先级固定为：

1. `PROJECT_BASELINE.md`
2. `STANDARD_PATCH_FRAMEWORK.md`
3. 单次聊天中的 patch 请求说明

## A. 硬规则摘要
- 一次只处理一个主问题。
- 每次 Patch 分类必须唯一：`PERF_ONLY / ARCH_ONLY / LOGIC_ONLY`。
- 必须先锁基线，再进入 Patch。
- 文件唯一身份只以 **MD5** 为准。
- `Lines` 只是辅助诊断，不是硬身份。
- 正式 Patch 必须：输入一致 + 输出一致。
- 若目标文件 `MD5` 不一致，必须立即中断。 
- “预期新指纹”必须是 **AI 在执行环境中真实运行 patch 后测得的结果**。
- 正式 Patch 一律按固定顺序交付：
  1. `patch方案`
  2. `patch脚本附件`
  3. `patch执行命令行`
  4. `预期新指纹`
  5. `一行 git add`
  6. `一行 git commit`

# 当前项目状态
（`CURRENT_STATE.md`）

## 0. 文档定位
本文件只记录当前现场快照，不重述宪法和 patch 框架已明确规定的内容。  
三者优先级固定为：

1. `PROJECT_BASELINE.md`
2. `STANDARD_PATCH_FRAMEWORK.md`
3. `CURRENT_STATE.md`

---

## 1. 当前项目定位

### 1.1 当前仓库
```text
bn_research_core
```

### 1.2 当前主线
```text
1. 让 SABC / Snapback 策略先毕业，进入 live 观察阶段
2. 启动新策略 Top1 Hunter（龙一猎手）研发
```

### 1.3 当前阶段
```text
Snapback/SAB 研究已阶段性收口；当前从“继续深挖 SAB 桶”切换到“策略毕业 + 双策略规划”阶段。
```

### 1.4 当前阶段目标
```text
1. 用当前最优基线让 SABC 毕业并准备 live
2. 持续做 SABC 的 sim/live 一致性审计
3. 开启 Top1 Hunter（顺势龙一）策略宪法与研发
4. 未来让逆势 SABC 与顺势 Top1 Hunter 并行运行，争取形成 regime 互补
```

---

## 2. 当前 Snapback / SABC 已确认结论

### 2.1 当前最优基线
```text
s_to_c_window = 60
market_total_24h_vol_min = 40B
election_rule = drop_pct_top1
```

### 2.2 选举规则结论
1. 在 `market_total_24h_vol_min = 40B` 的前提下，四轮 `election_rule` 全量对比已经完成。  
2. 当前表现最好的规则是：
```text
drop_pct_top1
```
3. `drop_pct_plus_vol_ratio_top1`、`drop_pct_plus_vol_ratio_plus_24h_vol_top1`、`vol_ratio_top1` 都已做过对照。  
4. 结论是：`election_rule` 有优化作用，但它不是穿透 2026 regime 的主轴。  

### 2.3 market_total_24h_vol_min 结论
1. `market_total_24h_vol_min` 的**语义归属**应视为 `universe`，不应视为 `structure`。  
2. sim 侧已经证明：该参数既有审计价值，也能带来明显提速。  
3. live 侧也应前移为更外层 gate；该 patch 方向已明确，但若后续再做，必须从当时最新干净基线重新开始。  
4. 当前可确认事实是：
```text
40B 是有效第一道外层筛网，但不是 2026 的终点答案。
```

### 2.4 SAB 研究当前结论
1. SAB 画像字段有信息量，尤其是：
- `ab_path_type`
- `depth_band`
- `a_peak_sharpness_band`
2. 但继续深挖后的现实是：
- 同样的 SAB 脸谱在 2025 和 2026 大面积正反翻面
- 即使再叠加 `market_total_24h_vol`，也没有找到一个“跨年份稳定同向”的硬桶
3. 因此当前结论是：
```text
SAB 更适合作为解释与审计工具，而不是当前阶段继续深挖的第一主线。
```
4. 当前不再把“继续细切 SAB 桶”放在第一优先级。  

---

## 3. 当前最重要的代码/审计事实

### 3.1 无漂移核对已完成
以下两组“新旧版本 / 新旧参数”在**可重叠时间范围内**都已核对完成，结论均为：
```text
核心交易逻辑没有漂移。
```

#### 60m 组
- `sim_all_o_60m.zip`
- `sim_all_z_60m.zip`

结论：
- `sim_signals` 集合一致
- `sim_trades` 集合一致
- 核心字段数值一致
- `z` 仅因时间范围更长，多出尾部少量样本

#### 21m 基线组
- `sim_all_j_21m基线.zip`
- `sim_all_A_21m.zip`

结论：
- `sim_signals` 集合一致
- `sim_trades` 集合一致
- 核心字段数值一致
- `A` 仅因时间范围更长，多出尾部少量样本

### 3.2 新增落盘字段已确认生效
当前新版本已确认：
- `market_total_24h_vol`
- SAB 画像相关字段
都已成功落盘。  
结论：
```text
只增强可观测性，不改变既有交易结果。
```

---

## 4. 当前对 Snapback 的战略判断

### 4.1 对 Snapback 的重新定位
当前应将 SABC / Snapback 明确视为：
```text
超跌反抽 / 逆势修复策略
```
它负责的不是“全年所有桌子”，而是：
- 大跌
- 暴量
- 错杀
- 反抽修复

### 4.2 当前现实判断
1. 2026 问题并未被彻底解决。  
2. `40B + drop_pct_top1` 已经把它拉到更可接受的状态，但没有得到“跨 regime 稳定穿越”。  
3. 因此当前更合理的策略不是继续无限期打磨，而是：
```text
先毕业、先实盘、先做一致性审计。
```

### 4.3 对后续 Snapback 修改的原则
后续对 Snapback 的修改应切换为：
```text
轻量迭代 / 必要修补 / 审计驱动
```
而不是重新回到“大规模结构探索阶段”。

---

## 5. 当前新主线：Top1 Hunter（龙一猎手）

### 5.1 新策略定位
Top1 Hunter 的目标是：
```text
抓顺势龙一，而不是抓超跌反抽。
```

### 5.2 与 SABC 的关系
两者世界观明确不同：

#### SABC
- 逆势
- 超跌
- snapback
- 修复

#### Top1 Hunter
- 顺势
- 龙头
- 扩散
- 延续

### 5.3 当前战略判断
当前非常重要的新判断是：
```text
不要再让单一策略硬跨所有 regime；而是让不同策略覆盖不同桌子。
```
因此后续目标不是“只靠 SABC 穿越 2026”，而是：
```text
让 SABC 与 Top1 Hunter 形成策略互补。
```

---

## 6. 当前 pending

### 6.1 Snapback / SABC pending
1. 以当前最优基线作为毕业候选：
```text
s_to_c_window = 60
market_total_24h_vol_min = 40B
election_rule = drop_pct_top1
```
2. 准备进入 live。  
3. 持续做 sim/live 一致性审计。  
4. 若后续还要动 `market_total_24h_vol_min` live 前移 patch，必须以当时真实干净基线重新锁指纹后再做。  

### 6.2 Top1 Hunter pending
1. 先不要急着写代码。  
2. 先明确策略宪法：
- 龙头定义
- 顺势定义
- 入场条件
- 离场逻辑
- 与 SABC 的边界
3. 待策略语义稳定后，再进入实现。  

---

## 7. 当前明确不做

- 不再把“继续细切 SAB 桶”作为当前第一主线
- 不再试图靠单一 SAB 黑名单直接解决 2026
- 不把 `election_rule` 继续当作当前突破主轴
- 不重新拉回旧的 candidate pool audit 主线
- 不在没有新事实时重复已完成的 60m / 21m 无漂移核对
- 不在未完成 Top1 Hunter 策略宪法前直接开写大段实现代码

---

## 8. 下一步顺序

```text
下一步顺序：
1. 明确 SABC 毕业基线与 live 启动清单
2. 进入 SABC live 观察 + sim/live 一致性审计
3. 并行开启 Top1 Hunter 策略宪法设计
4. 等两个策略都稳定后，再考虑双策略并行运行框架
```

---

## 9. 新聊天协作提醒

1. 新聊天不要再把当前主线误判为“继续深挖 SAB 桶”或“继续无限细化 Snapback 过滤器”。  
2. 当前已完成并收口的事实要直接继承：
- `drop_pct_top1` 是当前最优 `election_rule`
- `market_total_24h_vol_min = 40B` 是当前有效第一道外层筛网
- SAB 有信息量，但当前不再是第一优先级主线
3. 新聊天中若继续做正式 patch，必须继续严格遵守：
```text
输入一致 + patch 一致 + 输出一致
```
4. 正式 patch 的“预期新指纹”仍必须是 AI 在执行环境中真实运行 patch 后测得的结果。  
5. 当前最重要的新战略认知只有三条：
- SABC 应先毕业并进入 live 观察阶段
- 2026 的主导矛盾更偏 regime，而不是继续细切 SAB
- 新策略 Top1 Hunter 应尽快启动，以构建与 SABC 互补的双策略框架


---

## 10. Spring-SABC 新增基线与 1m 数据结构

当前新策略名称已确定为：

```text
spring-sabc
```

其定位不是旧称 Top1 Hunter 的泛化版本，而是已经成文的顺势延续结构策略：

```text
强势 universe + contract-only + 连续洗盘 AB + 放量 + 快速收回 BC + CB 入场
```

新增语义基线统一收敛到：

```text
docs/Spring-SABC项目语义基线.md
```

`docs/Spring-SABC_ABC结构定义.md` 已合并进该文件，后续仅作为旧引用入口保留。

### 10.1 Spring-SABC 当前核心语义

```text
LONG-only
contract-only
不使用 idx / idx_bars / basis
C = HBs[0]
CB = C + 1
signal_time = entry_time = CB
stop_loss_price = b_close
```

ABC 精确语义：

```text
从 C 向左搜索 B
B 初筛只要求 C_close > B_close
A 是 B 所属 close 严格连续下跌段的最早起点
AB 连跌只比较 close
B_low 必须是 A-B 区间最低 low
第一组完整满足条件的 A-B-C 是唯一结构
```

### 10.2 当前 1m 落盘数据结构

当前项目唯一有效的 1m 主表为：

```text
data/klines_1m/{SYMBOL}/{YYYY-MM}.parquet
columns: open_time_ms, open, high, low, close, quote_asset_volume, high_idx, low_idx, close_idx
```

这里的 `high_idx / low_idx / close_idx` 已并入 contract 主表。

`data/index_klines_1m/` 目前只可视为旧阶段残留目录，**不是**当前项目基线的一部分。  
后续在项目中：

```text
禁止新增任何读写 data/index_klines_1m/ 的实现
```

独立审计脚本优先复用：

```text
core.engine.data_feeder.CrossSectionalFeeder
```

若直接读 parquet，只允许按 `data/klines_1m/{SYMBOL}/{YYYY-MM}.parquet` 读取，禁止再猜旧路径。

### 10.3 当前阶段性结论

当前 Spring-SABC 已经完成：

```text
结构语义定稿
v1 代码实现
全量并行回测
viz 可视化复盘
pre-A 审计
γ-A / A-C 对称成交量审计
exit behavior 审计
BREAKEVEN_GUARD sim/live 语义澄清
```

当前已确认的阶段性结论：

```text
1. gamma_ac_vol_ratio_min = 1.0 有效，应保留
2. 当前最优主基线仍是 1924：gamma>=1 + 1R止盈 + max_hold_mins=60 + BREAKEVEN_GUARD disabled
3. BREAKEVEN_GUARD 代码语义已理顺，但当前阶段暂停启用；保留代码，配置关闭
4. Spring 当前主线从“继续细切 ABC 脸谱”转为“固定主基线 + 专项审计坏月份/坏 regime”
```

### 10.4 下一步主线

当前 Spring-SABC 下一步应按以下顺序推进：

```text
1. 把配置回退到 BREAKEVEN_GUARD disabled，重新固定 1924 语义为当前主基线
2. 专门审计 2026-04 为什么持续亏损
3. 继续保持 sim 图表、signals、trades、summary 全链路可追溯
4. 若后续再碰 BREAKEVEN_GUARD，只能在当前已澄清的 sim/live 语义上做新实验，禁止回到旧错误语义
```

### 10.5 新聊天发送建议

若下一个聊天的主线是 Spring-SABC，建议发送：

```text
1. docs/新聊天开场白.md
2. docs/Spring-SABC项目语义基线.md
```

不建议再单独发送：

```text
docs/Spring-SABC_ABC结构定义.md
```

原因：

```text
该文件内容已并入 Spring-SABC项目语义基线.md
当前仅作为旧引用入口保留
```


## 11. data_hub 当前状态（2026-04 新增）

### 11.1 当前结论

当前 data_hub 主链路已经进入：

```text
稳定可用
```

最近连续现场结果表明：

```text
1. hub_owned_1m_rollsum 已稳定 ready
2. >=5000万 USDT 候选池已稳定在 54~56 个附近波动
3. processed_symbols = 54~56
4. finalize_rounds = 5 或 6
5. deadline_hit = False
6. all_passed = True
7. timeout_not_finalized_count = 0
```

也就是说：

```text
全市场 24h_vol 刷新正常，candidate/finalize 在 54~56 个币规模下仍能稳定毕业。
```

### 11.2 当前 data_hub 已解决事项

```text
1. warmup -> ready 已打通
2. finalize round 内 deadline bug 已修复
3. finalize probe 误走重副作用链已修复
4. ready 后 24h_vol 自锁问题已修复
5. 全市场 refresh 曾导致 -1003 限频；当前主问题已被压住
6. 将全市场 rollsum refresh 移到 finalize 之后的 batch 路径后，55+ symbol 规模下 finalize 已恢复稳定
```

### 11.3 当前 data_hub 未解决 / 待继续观察

```text
1. market_snapshot.json 仍偏轻量，人工审计可观测性不足
2. prefilter 原始名单缺少清晰官方落盘字段
3. exclude_symbols 尚未接线
4. hub 24h_vol 与 Binance App 前端口径是否要求继续贴合，尚未定规
5. 是否长期彻底无 -1003，仍需继续观察
```

### 11.4 TradFi 当前处理结论

当前已确认：

```text
list_candidate_symbols(...) 不会把 TradFi 品种纳入候选 universe
```

已现场验证以下 TradFi 品种均不在当前 candidate universe 内：

```text
CLUSDT
BZUSDT
XAUUSDT
XAGUSDT
```

因此当前项目基线应明确为：

```text
sim / live 默认都排除 TradFi
```

理由：

```text
1. sim 是基线，live 必须向 sim 对齐
2. TradFi 是近月新增板块，若 live 纳入而 sim 历史 1m 未纳入，会直接破坏 universe.market_total_24h_vol_min 的回测意义
3. 典型加密资产与 TradFi 的波动基因不同，当前策略不需要对准 TradFi
```

### 11.5 1m 历史落盘当前结论

`klines_1m_store.py` 当前默认 universe 来自交易列表，再叠加：

```text
state/symbols_force_include.txt
```

也就是说：

```text
即使某个 symbol 不在默认交易列表里，只要它出现在 symbols_force_include.txt 中，1m 落盘仍会主动同步它。
```

当前已现场确认该文件曾包含：

```text
XAUUSDT
```

因此后续若要求：

```text
sim / live 默认都排除 TradFi
```

则必须同步遵守：

```text
禁止再通过 symbols_force_include.txt 把 TradFi 人工加回默认 1m 落盘 universe
```

### 11.6 新聊天若继续 data_hub 主线，建议优先顺序

```text
1. 先继续观察 data_hub 是否长期稳定无 -1003
2. 再补 market_snapshot / prefilter 的可观测性
3. 再决定是否要接 exclude_symbols
4. 暂不把“让 data_hub 纳入 TradFi”作为主线
```
