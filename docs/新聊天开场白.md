我们继续推进 `bn_research_core / strategies/snapback`。

以下 3 份文本内容构成当前对话唯一基线，请严格遵守：

1. `PROJECT_BASELINE.md`
2. `STANDARD_PATCH_FRAMEWORK.md`
3. `CURRENT_STATE.md`

优先级固定为：

1. `PROJECT_BASELINE.md`
2. `STANDARD_PATCH_FRAMEWORK.md`
3. `CURRENT_STATE.md`

请先完全按这三份内容协作，不要跳回旧主线，不要重复已完成 patch，不要发散扩展。

# 项目基线（宪法）
（`PROJECT_BASELINE.md`）

## 0. 总则
0.1 本基线适用于本项目内全部策略、全部 `sim / live / audit / patch / review / 对话协作`。  
0.2 若某策略需要例外，必须显式写入该策略文档；**未写明即禁止例外**。  
0.3 凡本基线未显式允许的行为，一律视为禁止。  

## 1. 公共语义

### 1.1 K线语义
1.1.1 全部 K 线统一分为：  
- `CB = Current Bar = 当前 bar = 编号 0`  
- `HBs = History Bars = 全部已收盘 bars = 编号 1..N`  

1.1.2 `CB` 永远只有唯一一根，OHLCV 会变化。  
1.1.3 `HBs` 全部已收盘，OHLCV 已固化不变。  
1.1.4 所有策略都必须在 **CB 时刻** 观察 **HBs**。  

### 1.2 时间字段语义
1.2.1 `signal_time` 只能表示信号生产时间。  
1.2.2 `signal_time` 必须严格等于 `CB` 的时间。  
1.2.3 `entry_time` 只能表示进场时间。  
1.2.4 `entry_time` 必须严格等于 `signal_time`。  
1.2.5 对本项目全部策略，`signal_time = entry_time = CB`。  

### 1.3 策略内部时间锚点
1.3.1 某些策略可能定义自己的内部结构时间锚点，例如 `A / B / C / S`。  
1.3.2 内部结构时间锚点（例如 `A / B / C / S`）必须全部归属于 HBs，即全部属于过去时。
1.3.3 这类内部时间锚点**不是**项目公共语义，禁止默认推广到全部策略。  
1.3.4 若某策略存在内部时间锚点，必须在该策略文档中显式定义其名称、含义、对应 bar 和对应字段。  
1.3.5 未在策略文档中显式定义的内部时间锚点，不得假定存在。  

### 1.4 价格语义
1.4.1 价格统一区分为：`contract` 与 `idx`。  
1.4.2 凡涉及价格字段，必须明确属于哪一类。  
1.4.3 禁止混用或偷换 `contract` 与 `idx`。  

### 1.5 配置语义
1.5.1 所有策略配置统一采用“配置段（section）”概念。  
1.5.2 当前公共配置段为：  
- `strategy_name`  
- `runtime`  
- `universe`  
- `structure`  
- `exit_policy`  
- `risk_controls`  

1.5.3 新增配置段时，必须显式说明职责；禁止隐式扩展旧段语义。  

### 1.6 sim / live 关系
1.6.1 `sim` 是策略语义基线。  
1.6.2 `live` 必须向 `sim` 对齐。  
1.6.3 若发现 `sim` 语义错误，必须先修 `sim`，再重跑验证，再要求 `live` 对齐。  
1.6.4 禁止用 `live` 现实限制掩盖 `sim` 语义错误。  

### 1.7 方向语义
1.7.1 本项目当前及未来统一只允许 LONG，不允许 SHORT。  
1.7.2 本项目不定义 SHORT 语义，不实现 SHORT 代码路径，不接受以 SHORT 为目标的 patch、review、audit、sim、live 设计。  
1.7.3 凡涉及方向的分析、对话、方案、代码实现、审计与交付，只讨论 LONG。  
1.7.4 未显式声明为 LONG-only 的字段、分支、行为，若引入 SHORT 语义，一律视为违规。  
1.7.5 禁止以“多空对称”“通用化”“未来扩展”为理由引入 SHORT 相关语义、字段、分支或实现。


### 1.8 策略账本语义
1.8.1 策略账本分为：  
- 当前账本  
- 历史账本  

1.8.2 当前账本定义为：  
- 仓位  
- 当前委托  
  - 基础单  
  - 条件委托  

1.8.3 历史账本定义为：  
- 历史委托  
- 历史成交  
- 资金流水  
- 仓位历史记录  

1.8.4 策略账本同时区分为：  
- 本地账本  
- BN账本  

1.8.5 本地当前账本可记为 `LCL = Local Current Ledger`。  
1.8.6 BN当前账本可记为 `BCL = Binance Current Ledger`。  

1.8.7 当前账本一致性必须区分两个方向：  
- `BN → Local`：BN当前账本有，而本地当前账本没有  
- `Local → BN`：本地当前账本有，而BN当前账本没有  

1.8.8 凡涉及“账本对齐”“账本一致性”“orphan”“reconcile”的分析、设计、审计与实现，若未显式说明范围，默认只讨论**当前账本**，不得偷换为历史账本。  

1.8.9 凡涉及当前账本对象，必须明确对象层级，不得用含混表述混淆：  
- 仓位  
- 当前委托  
  - 基础单  
  - 条件委托  

1.8.10 `audit_consumer_orphan_exchange_activity(...)` 的职责，定义为：  
以 `BCL` 为基线，审计是否存在“`BCL` 中已有、而 `LCL` 中未记录”的 orphan 场景；该职责只覆盖 `BN → Local` 缺失方向，不等于完整账本对齐。  

1.8.11 完整的当前账本对齐，目标定义为：  
以 `BPOS + BWO` 为当前账本基线，使 `LPOS + LWO` 对齐基线。  
其中：  
- `BPOS = Binance Positions`  
- `BWO = Binance Working Orders`  
- `LPOS = Local Positions`  
- `LWO = Local Working Orders`  

1.8.12 当前账本正文中允许直接使用以下核心缩写：  
- `LCL = Local Current Ledger`  
- `BCL = Binance Current Ledger`  
- `LPOS = Local Positions`  
- `BPOS = Binance Positions`  
- `LWO = Local Working Orders`  
- `BWO = Binance Working Orders`  

1.8.13 其余策略账本缩写，统一按下列规则解释：  
- 账本归属前缀：`L = Local`，`B = Binance`  
- 对象缩写：`CL = Current Ledger`，`HL = Historical Ledger`，`POS = Positions`，`WO = Working Orders`，`WBO = Working Base Orders`，`WCO = Working Conditional Orders`，`OH = Order History`，`FH = Fill History`，`IH = Income History`，`PH = Position History`  

1.8.14 术语附录：策略账本缩写表示例如下：  
- 当前账本：`LCL / BCL`  
- 当前账本对象：`LPOS / BPOS`，`LWO / BWO`，`LWBO / BWBO`，`LWCO / BWCO`  
- 历史账本：`LHL / BHL`  
- 历史账本对象：`LOH / BOH`，`LFH / BFH`，`LIH / BIH`，`LPH / BPH`  

## 2. 行动纪律（铁律）

### 2.1 事实优先
2.1.1 代码、日志、落盘文件、运行结果才是事实。
2.1.2 一切分析、结论、方案必须基于事实。  
2.1.3 禁止臆测。  
2.1.4 缺少事实时，必须先补齐事实，再下结论。    

### 2.2 fail-fast
2.2.1 必须坚持 `fail_fast`。  
2.2.2 禁止带病运行。  
2.2.3 配置缺失、字段缺失、语义冲突、数据异常时，必须中断并报错。  

### 2.3 禁止兜底 / 禁止硬编码 / 禁止伪兼容
2.3.1 禁止默认值兜底掩盖错误。  
2.3.2 配置必须显式提供；缺失就是错误。  
2.3.3 禁止业务语义硬编码。  
2.3.4 临时硬编码调试完成后必须删除。  
2.3.5 禁止向下兼容破坏当前业务语义。  
2.3.6 严禁兼容旧字段、旧字段残留、多套语义并存。  
2.3.7 一条语义只能对应一套字段、一套实现。  

### 2.4 patch 协作
2.4.1 正式 patch 的进入条件、分类约束、交付顺序、最小修改原则，统一严格服从 `STANDARD_PATCH_FRAMEWORK.md`。

### 2.5 表达纪律
2.5.1 提问和回答必须精准、简练。  
2.5.2 不遗漏关键线索。  
2.5.3 不发散、不啰嗦。  
2.5.4 先统一语义，再审代码。  

## 3. 审计纪律

### 3.1 审计工具原则
3.1.1 审计工具严禁猜字段。  
3.1.2 审计工具严禁兼容多个字段名。  
3.1.3 每条语义必须严格命中唯一字段。  
3.1.4 找不到唯一字段时，必须报错。  

### 3.2 审计基准
3.2.1 审计优先看落盘事实。  
3.2.2 单笔审计主键优先使用语义主键。  
3.2.3 审计必须区分：  
- 输入差异  
- 结构差异  
- 执行差异  
- 记账差异  

### 3.3 记账与逻辑分离
3.3.1 必须区分策略逻辑错误与记账字段错误。  
3.3.2 记账字段错误不等于策略逻辑错误。  
3.3.3 记账错误也必须修复，因为它会污染审计与判断。  

## 4. 代码与交付纪律

### 4.1 脚本交付
4.1.1 禁止在聊天窗口输出整段脚本。  
4.1.2 所有脚本、patch、diff 一律走附件。  

### 4.2 patch 脚本约束
4.2.1 patch 脚本不得包含 `/mnt/data/...`。  
4.2.2 patch 脚本必须基于已锁定指纹工作。  
4.2.3 patch 脚本必须在运行时校验基线指纹。  
4.2.4 指纹不一致时必须中断。  
4.2.5 文件唯一身份以 MD5 为准。
4.2.6 Lines 仅作为辅助诊断信息，不作为硬身份条件。

### 4.3 版本与验证
4.3.1 每次 patch 后必须验证：  
- 指纹  
- 编译 / 语法  
- 现场运行结果  

4.3.2 未验证通过，不得进入下一步。  
4.3.3 正式 patch 必须同时具备对齐输入与对齐输出。
4.3.4 未完成对齐输入或对齐输出，不算完整交付。

## 5. 判定优先级
5.1 实现便利性永远不能压过语义正确性。
5.1.1 当前阶段目标只能在不违反公共语义、fail-fast 与事实证据的前提下成立。
5.2 出现冲突时，优先级如下：  
- 公共语义  
- fail-fast  
- 事实证据  
- 当前阶段目标  
- 实现便利性  
  
# 标准化 Patch 协作框架
（`STANDARD_PATCH_FRAMEWORK.md`）

## 0. 文档定位

本文件用于规定 **Patch 协作的唯一标准流程**。  
它只回答一件事：

**一次 Patch 任务，应该如何发起、如何约束、如何交付。**

本文件不是项目基线（宪法），不替代：

- `PROJECT_BASELINE.md`

若本文件与宪法冲突，优先级固定为：

1. `PROJECT_BASELINE.md`
2. `STANDARD_PATCH_FRAMEWORK.md`
3. 单次聊天中的 patch 请求说明

---

## A. 硬规则

### A1. 一次只处理一个主问题
一次 Patch 只能处理一个主问题。  
禁止把多个无关问题混在一刀里。

### A2. Patch 分类必须唯一
每次 Patch 必须且只能属于以下之一：

- `PERF_ONLY`
- `ARCH_ONLY`
- `LOGIC_ONLY`

禁止混改。  
如确需跨类，必须拆刀顺序推进。

### A3. 必须先锁基线，再进入 Patch
进入 Patch 前，必须先锁定目标文件基线。  
未锁基线，不进入 Patch。

### A4. 文件唯一身份以 MD5 为准
文件唯一身份只以 **MD5** 为准。

### A5. Lines 不是硬身份
`Lines` 只作为辅助诊断信息，不作为硬阻塞条件。  
若 `MD5` 一致、`Lines` 不一致，不得仅因行数差异中断 Patch。

### A6. 不得基于未知版本 Patch
若目标文件 `MD5` 不一致，必须立即中断。  
不得默认“应该差不多”。  
不得基于未知版本继续 Patch。

### A7. Patch 必须最小外科式修改
只允许修改当前问题所必需的文件与代码。  
禁止顺手重构。  
禁止顺手清理无关代码。  
禁止顺手修改无关日志、命名、风格。

### A8. Patch 必须基于事实
Patch 方案必须基于以下事实之一或其组合：

- 代码事实
- 日志事实
- state 文件事实
- audit 文件事实
- 运行结果事实

缺少事实时，先补事实，不得臆测。

### A9. 正式 Patch 禁止在聊天框输出整段脚本
正式 Patch 的脚本、diff、patch 内容不得在聊天框直接整段输出。  
必须以附件方式交付。

### A10. Patch 输出顺序固定
每次要求 AI 直接输出 Patch 时，回答顺序必须固定为：

1. `patch方案`
2. `patch脚本附件`
3. `patch执行命令行`
4. `预期新指纹`
5. `一行 git add`
6. `一行 git commit`

不得缺项。  
不得改变顺序。

### A11. 预期新指纹必须给出
每次正式 Patch，必须给出 **预期新指纹**。

预期新指纹至少包含：

- 文件路径
- `MD5`
- `Lines`
- `Copy`

### A12. Patch 前后都必须可核对
每次正式 Patch，必须同时具备：

- **对齐输入**
- **对齐输出**

不得只给 Patch，不给输出指纹。  
不得只描述修改，不给可核对结果。

### A13. 一次性脚本与正式 Patch 必须区分
若文件只是一次性处理现场，不进入正式源码长期行为，则它不是正式 Patch。  
若修改项目源码并形成长期保留行为，则它属于正式 Patch，必须遵守本文件全部正式 Patch 规则。

---

## B. Patch 交付结构

每次要求 AI 直接输出正式 Patch，必须严格按以下结构回答。

### B1. patch方案
必须明确说明：

- 本刀分类
- 本刀只动哪些文件
- 本刀不动哪些文件
- 本刀改什么
- 本刀明确不改什么

### B2. patch脚本附件
必须给出附件文件名。  
不得在聊天框直接输出整段 Patch 脚本。

### B3. patch执行命令行
命令必须覆盖以下动作：

- 执行 patch
- `py_compile`
- 生成新的 `md5/line/copy`

### B4. 预期新指纹
必须按文件列出：

- 文件路径
- `MD5`
- `Lines`
- `Copy`

### B5. 一行 git add
只能 `add` 本刀涉及文件。  
不得把无关文件混入。

### B6. 一行 git commit
`commit message` 必须只描述本刀目标。  
不得混入其他问题。

---

## 结尾说明

本文件的唯一目标是：

- 降低重复沟通成本
- 降低跑偏概率
- 降低混改概率
- 保证 Patch 可核对
- 保证新聊天能稳定续接旧现场

本文件不追求“写得全面”，只追求：

**规则足够硬、结构足够稳、执行可核对。**

# 当前项目状态
（`CURRENT_STATE.md`）

## 0. 文档定位

本文件只用于记录 **当前现场状态**。  
它回答的是：

**我们当前正在做什么、已经做到哪里、下一步做什么。**

本文件不是：

- `PROJECT_BASELINE.md`
- `STANDARD_PATCH_FRAMEWORK.md`

三者分工固定为：

- `PROJECT_BASELINE.md`：原则、边界、禁令、优先级
- `STANDARD_PATCH_FRAMEWORK.md`：Patch 协作规则与交付结构
- `CURRENT_STATE.md`：当前现场快照

若本文件与前两者冲突，优先级固定为：

1. `PROJECT_BASELINE.md`
2. `STANDARD_PATCH_FRAMEWORK.md`
3. `CURRENT_STATE.md`

---

## 1. 当前项目定位

### 1.1 当前仓库
```text
bn_research_core
```

### 1.2 当前主线
```text
strategies/snapback
```

### 1.3 当前阶段
```text
审计 / 投影主线收束验证 + live 输入稳定性排查阶段
```

### 1.4 当前阶段目标
```text
在保持 live projection + bn truth + triplet audit 主线不发散的前提下，继续收束生产侧差异来源，重点确认 live 刚闭合 bar 的输入稳定性，并为双 live（高频审计 / 盈利验证）运行方式做准备。
```

---

## 2. 固定现场路径

```text
- live state：state/live/snapback_mybwin139.state.json
- audit jsonl：state/live_audit/snapback_mybwin139.jsonl
- live projection dir：output/live_projection
```

---

## 3. 当前唯一主线

```text
当前唯一主线：
继续推进并验证 live projection + bn truth + triplet audit 这条审计闭环主线，并对 live 生产侧输入稳定性做最小取证与收束。

当前优先级：
1. 巩固三方审计闭环（sim / live / bn）
2. 查清 live 侧刚闭合 bar 在首轮取数时是否未最终固化
3. 在不扩功能、不改策略逻辑前提下，增强当前基础设施稳定性
```

---

## 4. 最近已完成

1. `live_signals.<run_id>.jsonl` 与 `live_trades.<run_id>.jsonl` 已落地。
2. `bn_sync` 已可按样本 symbol 同步 `bn_orders / bn_fills / bn_income`。
3. `audit_trade_triplet_diff.py` 已能对单笔样本完成 sim / live / bn 联调审计。
4. `binance_exec.py` 已补充交易动作日志与 Telegram Bot 推送。
5. live 侧已补充：
   - 雷达锁定消息推送
   - 离场日志与 Bot 推送
   - 离场消息持仓时间显示
6. 已完成 3 笔真实 live 样本的首轮 triplet audit。
7. 已完成 `selected_tp_pct` 字段完整性修补，恢复链路中的 `open_trade` 现可保留 `selected_tp_pct / entry_price_source / resolved_tp_price_source`。
8. 已完成对 SIRENUSDT 风险现场的事实分析，确认问题根因为：ENTRY 成交后 SL 无法建立保护，导致出现“有持仓、有TP、无SL”的风险现场。
9. 已完成 live 侧 LONG 风险修补：增加开仓前 SL 预筛，并在 ENTRY 后按 `ENTRY -> SL -> TP` 顺序执行；若 SL 无法建立保护，则立即 fail-fast 退出。
10. 已完成对 `trade_consumer.py` 中 ENTRY 后立即 bracket check / repair 冗余链路的事实审计，并确认：在 `ENTRY -> SL -> TP` 已成功提交的场景下，这条 repair 链会制造额外复杂性与噪音，已形成删除该链路的 patch 方案并完成版本收束。
11. 已完成对生产侧审计工具职责的重新收束：
    - `window_diff` 用于批量发现问题
    - `snapshot_diff` 用于打穿单笔 signal
    - `triplet_diff` 用于消费侧闭环审计
12. 已完成对 `FARTCOINUSDT` 样本的逐层取证，确认 live 侧 `01:42:01` 这一轮读取到的 `01:41` bar 在 `stage3_enriched / stage5_structure_audit / live_signals` 中均为 `0.1699`，而后续轮次同一根 `01:41` bar 又修正为 `0.1701`。
13. 已确认 `FARTCOINUSDT` 样本中的异常不属于下游 projection 写坏，而是更上游 live 输入链在 bar 刚闭合后的首轮取数中拿到了尚未最终固化的 closed bar。
14. 已完成对 `BEATUSDT` live-only 样本的排查，确认是与FARTCOINUSDT相同的原因即“在 bar 刚闭合后的首轮取数中拿到了尚未最终固化的 closed bar”。
15. 已确认当前 runner 固定在每分钟第 1 秒开始取 candidate market data；`FARTCOINUSDT` 异常样本的 `candidate_md_started_bj = 2026-04-02 01:42:01`，与“刚闭合 bar 首轮取数过早”假设一致。
16. 已完成双 live 运行方式的可行性确认：同一台服务器、同一虚拟环境可同时启动两个独立 live 进程，只要使用不同账户、不同 `config/live_config`、不同日志文件即可。

---

## 5. 当前已验证通过

1. live projection 主线可用：`live_signal` 与 `live_trade` 均可稳定落盘。
2. bn truth 主线可用：`bn_orders / bn_fills / bn_income` 可围绕指定 symbol 成功补齐。
3. triplet audit 主线可用：已成功审计 3 笔真实样本。
4. 三笔样本的离场原因三方一致：
   - ONUSDT：`STOP_LOSS`
   - AIAUSDT：`STOP_LOSS`
   - BRUSDT：`TAKE_PROFIT`
5. live 与 bn 的入场价格已对齐。
6. live 与 bn 的离场价格已实质对齐；少量差异属于浮点精度差异。
7. 当前审计主锚点可优先使用：
   - `client_order_id`
   - `order_root`
   - `leg`
8. 条件单场景下，已确认：
   - live 侧记录的 `exit_order_exchange_id` 可能是条件单 / algo 父单 ID
   - bn `exchange_order_id` 记录的是最终基础成交子单 ID
   - 父单 / 子单当前样本中保留相同 custom_id，可用于稳定认亲
9. 已确认 LONG 场景下，STOP_MARKET 止损单能否成功提交，最终取决于提交当下的最新价格是否会导致 “would immediately trigger”，而不是取决于 entry_price。
10. 已确认 `FARTCOINUSDT` 这笔共同样本的 structure 差异、以及`BEATUSDT` live-only 问题，都是同一个原因即 live 输入层在首轮取数时读取到了尚未最终固化的 closed bar。
11. 已确认 `sim_signals` 与 `live_signals` 中 `current_price / c_price` 字段存在语义混写风险；其中 `sim` 最终成交价仍由 broker 按 `CB.open` 成交，不直接受 signal snapshot 中该字段歧义影响。
12. 已确认同一虚拟环境同时运行两个 live 进程在工程上可行，隔离前提是：不同账户、不同配置文件、不同日志文件。

---

## 6. 当前 pending

1. 是否为 bn truth 增加“条件委托 / algo 父单”独立真相层，尚未决定。
2. triplet audit 后续是否要把“SL 父单 ID ≠ 基础子单 ID”显式纳入报告解释层，尚未决定。
3. 需要形成当前阶段的正式结论归档（验证通过项 / 待改进项）。
4. 需要验证把 live runner 调度点从“每分钟第 1 秒”后移到“每分钟第 2 秒”后，是否能显著降低刚闭合 bar 首轮取数未固化的问题。

6. 需要评估当前单轮 live 循环耗时偏长的问题：从 `candidate_md_started_bj` 到 `signal_eval_finished_bj` 在异常样本中已接近 50 秒，存在逼近下一分钟边界的风险。
7. 需要决定是否为双 live（高频审计 / 盈利验证）补充 profile 级运行标识与更清晰的日志/落盘区分，但这属于后续可选增强，不是当前启动双 live 的前置条件。

---

## 7. 当前明确不做

- 不扩新功能
- 不主动改策略逻辑
- 不主动重构 `bn_sync`
- 不回到旧主线问题
- 不重复已完成并已 push 的 patch
- 不把当前阶段变成大规模结构重构

---

## 8. 下一步

```text
下一步顺序：
1. 评估当前单轮 live 循环耗时偏长的问题：从 `candidate_md_started_bj` 到 `signal_eval_finished_bj` 接近 50 秒，存在逼近下一分钟边界的风险。
2. 形成当前阶段结论归档（验证通过项 / 待改进项）
3. 继续决定 bn truth 是否增加条件委托 / algo 父单独立真相层
4. 视运行稳定性，启动双 live：高频审计 live + 盈利验证 live
```

---

## 9. 当前协作提醒

1. 当前阶段优先“验证与收束”，不是继续发散扩展。
2. 审计主键优先使用 `custom_id / order_root / leg`，不要硬要求父单 ID 与子单 ID 相等。
3. 正式 Patch 必须继续遵守：
   - 先锁基线
   - 固定输出顺序
   - 对齐输入 + 对齐输出
4. 文件唯一身份以 `MD5` 为准；`Lines` 只作辅助诊断。
5. 本项目当前及未来统一只允许 LONG，不允许引入 SHORT 语义、字段、分支或实现。
6. 当前新发现的核心审计认识是：live 侧在 bar 刚闭合后的首轮取数，可能拿到尚未最终固化的 closed bar；后续对 sim/live 生产侧差异的判断，必须优先区分“universe 差异”“输入层时点差异”“真正的 structure 差异”。
7. 双 live 方案的工程前提已明确：不同账户、不同 `config/live_config`、不同日志文件；在未满足隔离前提下，不要共用同一个账户去跑两个 live。
