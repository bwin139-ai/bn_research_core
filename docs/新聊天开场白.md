# Snapback 新聊天开场白

我们继续推进 `bn_research_core / strategies/snapback`。

请把以下内容视为**本对话的唯一现场基线与协作框架**。  
必须严格遵守：

- 《项目基线（宪法）v1》
- 《标准化 Patch 协作框架 v1》

若二者冲突，以《项目基线（宪法）v1》优先。

---

## 一、固定现场路径（已确认）

- `live state`：`state/live/snapback_mybwin139.state.json`
- `audit jsonl`：`state/live_audit/snapback_mybwin139.jsonl`
- `live projection dir`：`output/live_projection`

---

## 二、当前主线

当前主线**不是**再做新的策略逻辑修改，而是继续推进并验证这条**审计 / 投影主线**：

1. `live_signals.<run_id>.jsonl`
2. `live_trades.<run_id>.jsonl`
3. `bn` 交易所真相层
4. `sim / live / bn` 三方审计闭环

其中：

- `signals` 审计：**只看 sim vs live**
- `trades` 审计：**看 sim vs live vs bn**
- `bn` 层**不做 signals**，只做交易所真相

---

## 三、当前已完成到的阶段

### 1）live projection 主线已落地

已具备：

- `live_signals.<run_id>.jsonl`
- `live_trades.<run_id>.jsonl`

### 2）bn 真相层已具备基础供应能力

已覆盖：

- `orders`
- `fills`
- `income`
- `position snapshots / position facts`

### 3）三方交易审计工具已落地

- `audit_tools/sim_live_audit/audit_trade_triplet_diff.py`

### 4）当前最新基线中，以下这一刀已完成并已提交

- `live projection + triplet audit v2`

对应提交说明：

- `audit: promote live projections to first-class triplet inputs`

---

## 四、当前协作原则

1. **先事实，后结论**
2. **先锁指纹，再谈 patch**
3. `patch` 必须明确分类：
   - `PERF_ONLY`
   - `ARCH_ONLY`
   - `LOGIC_ONLY`
4. **一次只处理一个主问题**
5. **禁止混改**
6. **禁止在聊天窗口输出整段脚本**，脚本 / patch / diff 一律走附件
7. **未经确认，不要重复输出已经完成并已 push 的同一刀 patch**

---

## 五、当前阶段目标

当前优先级不是继续盲目 patch，而是先做**首轮联调验证**，确认这条链是否真正闭环。

### 第一步

跑 `live`，确认稳定落盘：

- `live_signals.*.jsonl`
- `live_trades.*.jsonl`

### 第二步

跑 `bn_sync`，确认稳定落盘：

- `bn_orders`
- `bn_fills`
- `bn_income`
- `bn_position_facts`

以及对应 `raw` 层。

### 第三步

选 `1~2` 笔真实样本，跑：

- `audit_trade_triplet_diff.py`

### 第四步

基于联调结果，再决定是否继续 patch。

---

## 六、当前默认判断

在首轮联调结果出来前：

- 不要主动扩新功能
- 不要主动改策略逻辑
- 不要主动重构 `bn_sync`
- 不要重复输出已经完成的 patch

---

## 七、如果接下来需要 patch

请严格按《标准化 Patch 协作框架 v1》的格式回答，顺序固定为：

1. `patch方案`
2. `patch脚本附件`
3. `patch执行命令行`
4. `预期新指纹`
5. `一行 git add`
6. `一行 git commit`

---

## 八、当前最需要你记住的事

这条线的核心目标是：

把 `live_signals / live_trades / bn 真相层`  
变成**稳定、可复核、可长期使用**的审计基础设施，  
然后再用它们去做 `sim / live / bn` 一致性验证。

**不要跳回旧问题，不要重复旧 patch，不要发散。**

---

## 九、新聊天进入后的第一条回答应做什么

请先根据当前阶段，告诉我：

1. 现在最合理的下一步验证动作是什么
2. 需要我提供哪些最新落盘文件 / 样本
3. 如果还需要 patch，只能动哪几个文件