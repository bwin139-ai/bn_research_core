我们继续推进 `bn_research_core`。

请先不要依赖任何历史聊天记忆，按仓库当前文档恢复项目状态：

1. 运行 `git status --short`
2. 阅读 `AGENTS.md`
3. 阅读 `docs/README.md`
4. 阅读 `docs/PROJECT_BASELINE.md`
5. 阅读 `docs/STANDARD_PATCH_FRAMEWORK.md`
6. 阅读 `docs/CURRENT_STATE.md`

如果本轮任务涉及某个具体策略，再补读对应策略语义文档。例如 Spring-SABC：

```text
docs/Spring-SABC项目语义基线.md
```

协作原则：

1. 以代码、日志、state 文件、落盘输出和命令结果为事实。
2. 不把旧聊天、旧归档、旧报告当成当前主线。
3. 正式 patch 必须先锁基线，一次只处理一个主问题。
4. 不引入 SHORT 语义。
5. 未经批准，不 `git push`，不碰生产发布。

请先简要复述你读到的当前项目状态，再开始执行本轮任务。
